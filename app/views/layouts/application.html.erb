<!DOCTYPE html>
<html>
<head>
  <title><%= meta_title %></title>
  <meta name="description" content="<%= meta_description %>">

  <!-- Facebook Open Graph data -->
  <meta property="og:title" content="<%= meta_title %>" />
  <meta property="og:type" content="website" />
  <meta property="og:url" content="<%= request.original_url %>" />
  <meta property="og:image" content="<%= meta_image %>" />
  <meta property="og:description" content="<%= meta_description %>" />
  <meta property="og:site_name" content="<%= meta_title %>" />
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <!-- <title>Chip In</title> -->
  <link rel="icon" type="image/png" href="https://res.cloudinary.com/dqlvehu5z/image/upload/v1583424588/CHIP-IN/Charities%20pics/world_cjvsmh.png" />
  <%= csrf_meta_tags %>
  <%= action_cable_meta_tag %>
  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= stylesheet_pack_tag 'application', media: 'all' %>
</head>
<body>
  <div class="content">
    <% if (params[:controller] == 'pages' && params[:action] == "dashboard")  %>
      <% current_user.notifications = 0%>
      <% current_user.save %>
    <% end %>
    <%= render "shared/navbar" %>
   <%= render "shared/flashes" %>
    <%= yield %>
    <div class="push"></div>
  </div>
    <%= render "shared/footer" %>

  <%= javascript_include_tag 'application' %>
  <%= javascript_pack_tag 'application' %>
</body>
</html>
