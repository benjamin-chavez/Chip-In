## Chip-In Project Setup:

#### Setup Dependencies:

```cmd
 $ bundle install
 $ yarn
```

#### In one terminal Run Webpacker:

```cmd
 $ webpack-dev-server
```

#### In a second terminal run the rails server:

```cmd
 $ rails s
```
